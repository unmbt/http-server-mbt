# Compatibility entry point. C ABI acceptance belongs to the same candidate
# run as native tests, CLIs, containers and all external consumers.
name: C ABI candidate acceptance
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  candidate:
    uses: ./.github/workflows/cli.yml
