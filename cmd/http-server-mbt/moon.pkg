import {
  "unmbt/http-server-mbt/cmd/common",
  "unmbt/http-server-mbt/full" @server,
  "moonbitlang/async",
}
pkgtype(kind: "executable")

options(link: { "native": { "cc-flags": "${build.HS_C_FLAGS}", "cc-link-flags": "${build.HS_CLI_LINK}" } })
